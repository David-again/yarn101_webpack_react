import React from 'react';
import { Switch, BrowserRouter as Router, Route } from 'react-router-dom';

const TestComp = () => {
  return (
    <div>
        <p className="color-red big-text">I am a test component for React</p>
    </div>
  );
};

export default TestComp;